---
id: include
title: include
---

Directs the executor to include this field or fragment only when the `if` argument is true.

```graphql
directive @include(
  if: Boolean!
)
```

### Arguments

#### `if` ([`Boolean`](../../order-view-service/scalars/boolean))

Included when true.
